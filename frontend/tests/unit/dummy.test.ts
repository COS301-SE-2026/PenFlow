// Delete this file, it's simply being used to test CI/CD

describe("Dummy Suite", () => {
  it("1 + 1 equals 2", () => {
    expect(1 + 1).toBe(2);
  });

  it("hello does equal hello", () => {
    expect("hello").toBe("hello");
  });
});
